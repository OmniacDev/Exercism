#if !defined(TRINARY_H)
#define TRINARY_H

#include <string>
#include <cmath>

using namespace std;

namespace trinary {
    int to_decimal(const string& trinary);
}  // namespace trinary

#endif // TRINARY_H