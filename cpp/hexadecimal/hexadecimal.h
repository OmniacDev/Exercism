#if !defined(HEXADECIMAL_H)
#define HEXADECIMAL_H

#include <string>
#include <cmath>

using namespace std;

namespace hexadecimal {
    int convert(const string& str);
}  // namespace hexadecimal

#endif // HEXADECIMAL_H